#include <inttypes.h>
#include "lib/file.h"

void bitboard_write_string(uint64_t value)
{
    for (int rank = 0; rank < 8; rank++)
    {
        for (int file = 0; file < FILES; file++)
        {
            
        }
    }
}
